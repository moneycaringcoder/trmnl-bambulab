{
  "name": "trmnl-bambulab-bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local bridge that fuses Bambu Lab LAN MQTT and Bambu Cloud telemetry into a TRMNL webhook payload.",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "setup": "node src/setup/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "tsc -p tsconfig.json --noEmit",
    "scan": "../scripts/secret-scan.sh --tree"
  },
  "dependencies": {
    "mqtt": "^5.10.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
